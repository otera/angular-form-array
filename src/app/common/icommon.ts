import { FormGroup } from "@angular/forms";
export interface questionSetting {
  question: string;
  placeholder: string;
}
export interface ciFormSetting {
  formGroup: FormGroup;
  placeholder: string;
}
